command_prefix: valint bom
variable_mapping:
  TARGET:
  ATTACH_REGEX: --attach-regex
  COMPONENTS: --components
  ENV: --env
  FORCE: --force
  FORMAT: --format
  PACKAGE_EXCLUDE_TYPE: --package-exclude-type
  PACKAGE_GROUP: --package-group
  PACKAGE_TYPE: --package-type
  ATTEST_CONFIG: --attest.config
  ATTEST_DEFAULT: --attest.default
  BACKOFF: --backoff
  CACHE_ENABLE: --cache-enable
  CONFIG: --config
  CONTEXT_DIR: --context-dir
  FILTER_REGEX: --filter-regex
  FILTER_SCOPE: --filter-scope
  GIT_BRANCH: --git-branch
  GIT_COMMIT: --git-commit
  GIT_TAG: --git-tag
  LABEL: --label
  LEVEL: --level
  OCI: --oci
  OCI_REPO: --oci-repo
  OUTPUT_DIRECTORY: --output-directory
  OUTPUT_FILE: --output-file
  PRODUCT_KEY: --product-key
  SCRIBE_AUTH_AUDIENCE: --scribe.auth.audience
  SCRIBE_CLIENT_ID: --scribe.client-id
  SCRIBE_CLIENT_SECRET: --scribe.client-secret
  SCRIBE_ENABLE: --scribe.enable
  SCRIBE_LOGIN_URL: --scribe.login-url
  SCRIBE_URL: --scribe.url
  TIMEOUT: --timeout
  VERBOSE: --verbose
