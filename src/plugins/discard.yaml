command_prefix: valint discard
variable_mapping:
  TARGET:
  CURRENT: --current
  INTERACTIVE: --interactive
  REF: --ref
  SILENT: --silent
  STORE: --store
  CACHE_ENABLE: --cache-enable
  CONFIG: --config
  DELIVERABLE: --deliverable
  ENV: --env
  GATE: --gate
  LABEL: --label
  LEVEL: --level
  LOG_CONTEXT: --log-context
  LOG_FILE: --log-file
  OUTPUT_DIRECTORY: --output-directory
  OUTPUT_FILE: --output-file
  PIPELINE_NAME: --pipeline-name
  PREDICATE_TYPE: --predicate-type
  PRODUCT_KEY: --product-key
  PRODUCT_VERSION: --product-version
  SCRIBE_CLIENT_ID: --scribe.client-id
  SCRIBE_CLIENT_SECRET: --scribe.client-secret
  SCRIBE_DISABLE: --scribe.disable
  SCRIBE_ENABLE: --scribe.enable
  SCRIBE_URL: --scribe.url
  STRUCTURED: --structured
  TIMEOUT: --timeout
  VERBOSE: --verbose
